{
	"name": "skull-king",
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "cd src/main/webapp && xdg-open http://localhost:5173 && npm run dev",
		"buildDev": "cd src/main/webapp && npm run build"
	}
}
